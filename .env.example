# ==========================================
# Chess Results Backend - Environment Variables
# ==========================================

# MongoDB Connection
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Admin Password
ADMIN_PASSWORD=your_secure_admin_password_here

# Frontend URL (where arbiter links will point to)
# Development: http://localhost:5173
# Production: https://your-frontend.onrender.com
BASE_URL=http://localhost:5173

# Server Port (Render will set this automatically in production)
PORT=5000

# Node Environment
NODE_ENV=development

# Frontend URL for CORS (same as BASE_URL in most cases)
FRONTEND_URL=http://localhost:5173

# Puppeteer Configuration
# The full 'puppeteer' package is used which bundles its own Chromium.
# No PUPPETEER_EXECUTABLE_PATH needed - it auto-detects the bundled browser.
# Ensure PUPPETEER_CACHE_DIR is set on Render for persistent caching:
# PUPPETEER_CACHE_DIR=/opt/render/.cache/puppeteer
